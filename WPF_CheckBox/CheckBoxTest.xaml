<Window x:Class="WPF_CheckBox.CheckBoxTest"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_CheckBox"
        mc:Ignorable="d"
        Title="CheckBoxTest" Height="450" Width="800">
        <!--
        CheckBox控件继承自ToggleButton类，这意味着用户可切换他们的开关状态

        CheckBox类没有添加任何成员，所以CheckBox类的基本接口是在ToggleButton类中定义的

        IsChecked属性是可空的Boolean类型，这意味着该属性可以设置为true、false或者null

        IsThreeState属性决定了用户是否能将复选框设置为不确定状态
        如果IsThreeState属性被设置为false(默认值)，单击复选框时，其状态会在选中和未选中两种状态之间切换，并且
        这时只能通过代码将复选框设置为不确定状态
        如果IsThreeState属性被设置为true，单击复选框时，就会在所有可能的三种状态之间循环切换

        ToggleButton类还定义了当复选框进入特定状态时会触发的三个事件：Checked、UnChecked和Indeterminate。大多数情况下，
        可以很容易地通过处理继承自ButtonBase类的Click事件，将这一逻辑合并为单个事件处理程序。无论何时改变按钮的状态都会
        触发Click事件
    
    -->
    <Grid>
        <CheckBox IsChecked="{x:Null}">A check box in indeterminate state
        </CheckBox>
    </Grid>
</Window>
