<Window x:Class="WPF_ProgressBar.IndeterminateProgressBar"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_ProgressBar"
        mc:Ignorable="d"
        Title="IndeterminateProgressBar" Height="300" Width="300">

    <!--
    ProgressBar控件指示长时间运行任务的进度。与Slider控件不同，ProgressBar控件不能与用户进行交互。
    反而，需要由代码递增Value属性值(从技术角度看，WPF规则建议不讲ProgressBar作为控件，因为他无法响应鼠标动作和键盘输入)

    ProgressBar控件具有4个设备无关单位的最小高度。如果希望看到更大更传统的进度条，需要设置Height属性(或将它放入具有适当固定尺寸的容器中)

    使用ProgressBar控件的通常方式是将它作为长时间运行的状态指示器，甚至可能不知道该任务需要执行多长时间。可通过将IsIndeterminate属性设置
    为true来完成这一工作

    当设置IsIndeterminate属性时，不再使用Minimum、Maximum和Value属性。ProgressBar控件会周期性地显示从左向右跳动的绿色脉冲，这是通用的Windows约定，表示
    工作正在进行中。
    -->
    <StackPanel Margin="5">
        <ProgressBar Height="10" IsIndeterminate="True"/>
    </StackPanel>
</Window>
