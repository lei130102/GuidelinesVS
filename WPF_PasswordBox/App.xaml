<Application x:Class="WPF_PasswordBox.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WPF_PasswordBox"
             StartupUri="SimplePasswordBox.xaml">
    <!--

             WPF提供了三个用于输入文本的控件：TextBox、RichTextBox和PasswordBox

             PasswordBox控件直接继承自Control类
             TextBox和RichTextBox控件间接继承自TextBoxBase类


    
    
             与内容控件不同，文本框能够包含的内容类型是有限的

             TextBox控件总是存储字符串(由Text属性提供)
             PasswordBox控件也处理字符串内容(由Password属性提供)，尽管为了减轻特定类型的攻击，他在内部使用SecureString属性
             只有RichTextBox控件可存储更复杂的内容：可包含复杂元素组合的FlowDocument对象
    -->
    <Application.Resources>
         
    </Application.Resources>
</Application>
